# Forms

## Trigger
需要处理与Web 组件构建相关的任务时

## Output Format
结构化结果

## Standard Steps
1. Run the initialization script to create a new React project:
2. bash scripts/init-artifact.sh <project-name>
3. cd <project-name>
4. This creates a fully configured project with:
